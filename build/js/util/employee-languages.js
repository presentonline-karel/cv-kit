/*const employeeLanguages = () => {
    const progBarWrappers = document.querySelectorAll(".prog-bar-wrapper");
    const progBars = document.querySelectorAll(".prog-bar");
    const progBarPercentages = document.querySelectorAll(".language-percentage-number");
    const leftSides = document.querySelectorAll(".left-side");
    const rightSides = document.querySelectorAll(".right-side");

    for (let i = 0; i < progBarWrappers.length; i++) {
        //progBarWrappers[i].style.cssText += `draw-progress(${progBarPercentages[i].innerHTML});`;

        let percentage = progBarPercentages[i].innerHTML;

        leftSides[i].style.transform = `rotate(${percentage} * 3.6deg);`;

        // if(progBarPercentages[i] <= 50) {

        // }
    }
}

employeeLanguages(); */